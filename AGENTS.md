See [CLAUDE.md](./CLAUDE.md).

For Codex: when a task may depend on project-specific Claude commands, skills, or workflows, also inspect `.claude/` (especially `.claude/commands/` and `.claude/skills/` if present) and reuse the relevant instructions.
